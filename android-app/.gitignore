# 依赖
node_modules/

# Capacitor 生成的原生工程（由 `cap add android` 生成，不应入库；
# 它体积大且含平台相关配置，应在 CI / 开发者机器上按需重建）
android/

# 本地构建产物
*.apk
*.aab
.gradle/
local.properties

# 内置服务器地址（个人域名，严禁入库！仅用于本地构建注入安卓产物）
# 内容形如：https://your-server.example.com
# 也可用环境变量 TE_SERVER_URL 替代此文件。
.te-server-url
